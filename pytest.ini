[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = -v --tb=short --color=yes
markers =
    integration: 需要外部依赖（Docker / 网络 / LLM）
    slow: 慢速测试（> 5s）
    contract: 契约测试（防 schema 漂移）