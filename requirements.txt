# ============================================================
# Coding Agent — 后端依赖
# Python >= 3.11
# ============================================================

# ---------- 核心框架 ----------
langchain>=1.0,<2.0
langchain-core>=0.3
langgraph>=0.3
deepagents>=0.1
pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0
pyjwt[crypto]>=2.9
cryptography>=43.0
# ---------- 模型 Provider ----------
langchain-openai>=1.0
langchain-anthropic>=1.0
langchain-deepseek>=0.1
langchain-community>=0.3
openai>=1.50
anthropic>=0.40

# ---------- 代码库理解 ----------
tree-sitter>=0.23
tree-sitter-python>=0.23
networkx>=3.0
pathspec>=0.12
pyyaml>=6.0
tomli>=2.0; python_version < "3.11"

# ---------- 向量索引与检索 ----------
chromadb>=0.5
langchain-chroma>=0.2
sentence-transformers>=3.0
tiktoken>=0.7
faiss-cpu>=1.8; platform_machine != "arm64"

# ---------- 沙箱与执行 ----------
docker>=7.0
pexpect>=4.9
anyio>=4.6

# ---------- 状态与记忆 ----------
langgraph-checkpoint-sqlite>=2.0
langgraph-checkpoint-postgres>=2.0
psycopg[binary]>=3.1
psycopg-pool>=3.2
sqlalchemy>=2.0

# ---------- MCP ----------
langchain-mcp-adapters>=0.1
mcp>=1.24.0,<2.0.0
fastmcp>=2.0
GitPython>=3.1

# ---------- API 服务 ----------
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
sse-starlette>=2.1

# ---------- 可观测性 ----------
langsmith>=0.2
structlog>=24.0
rich>=13.9
typer>=0.13
click>=8.1

# ---------- 工具库 ----------
httpx>=0.27
requests>=2.32
tenacity>=9.0
orjson>=3.10

# ---------- 异步 ----------
asyncio>=3.4.3; python_version < "3.11"
aiofiles>=24.1

# ============================================================
# 开发依赖（可选）
# ============================================================
# 如果用 requirements-dev.txt 分开管理，把下面这一段
# 放到 requirements-dev.txt 里，然后用：
#   pip install -r requirements.txt -r requirements-dev.txt

# ---------- 测试 ----------
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=6.0
pytest-mock>=3.14
pytest-xdist>=3.6
pytest-timeout>=2.3
freezegun>=1.5

# ---------- 代码质量 ----------
ruff>=0.7
mypy>=1.13
black>=24.10
isort>=5.13

# ---------- 类型存根 ----------
types-requests>=2.32
types-pyyaml>=6.0
types-docker>=7.0